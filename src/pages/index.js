import { Homepage } from "@/components";
import PublicLayout from "@/layouts/public";

const index = () => {
  return (
    <PublicLayout>
      <Homepage />

    </PublicLayout>
  );
};

export default index;
